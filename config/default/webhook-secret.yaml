# uncomment lines below and provide tsl.crt and tls.key with sensible data. Alternatively use certmanager to create the secret
#apiVersion: v1
#kind: Secret
#type: kubernetes.io/tls
#metadata:
#  name: webhook-server-cert
#  namespace: system
#data:
#  tls.crt: Cg==
#  tls.key: Cg==
